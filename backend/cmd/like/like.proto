syntax = "proto3";

package like;
option go_package="./like";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Like {
  rpc Ping(Request) returns(Response);
}
