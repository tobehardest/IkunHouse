syntax = "proto3";

package video;
option go_package="./video";

message UploadVideoReq {
  bytes avatar = 1;
}

message UploadVideoRes {
  string avatar = 1;
}

service Video {
  rpc UploadVideo(UploadVideoReq) returns(UploadVideoRes);
}
